import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'medisphere-mobile-angular-front',
  webDir: 'www'
};

export default config;
